import React, { useState, useContext } from "react";

const UseContext = () => {
  return (
    <div>
      <h4>Use Context</h4>
      {/* There are two things in context  */}
    </div>
  );
};

export default UseContext;
