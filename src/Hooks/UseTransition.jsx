import React from "react";

const UseTransition = () => {
  return (
    <div>
      <h2 className="mb-4 text-2xl font-bold">USE TRANSITION</h2>
      <p>
        Using transitions in React can add smooth animations to your
        application, making it more visually appealing and enhancing the user
        experience. One common library used for animations and transitions in
        React is react-transition-group.
      </p>
    </div>
  );
};

export default UseTransition;
